# PROPOSAL-2025-0085: Исправление пути к данным по умолчанию в Analyzer

## Диагностика
Я допустил ошибку в интерпретации предыдущего запроса. Я изменил путь записи данных в проекте `collections`, в то время как проблема заключалась в пути чтения данных в проекте `analyzer`.

В данный момент скрипт `analyzer/run_all_ultra.py` использует относительный путь по умолчанию `../data/market_data` для аргумента `--data-path`. Это не соответствует актуальному расположению данных и требует ручного указания абсолютного пути при каждом запуске.

## Предлагаемое изменение
Предлагается изменить значение по умолчанию для аргумента `--data-path` в файле `analyzer/run_all_ultra.py` на абсолютный путь `C:\visual projects\arb1\data\market_data`.

### Изменения в `analyzer/run_all_ultra.py`
```python
<<<<<<< SEARCH
:start_line:605
-------
    parser.add_argument("--data-path", type=str, default="../data/market_data",
                        help="Path to the market data directory.")
=======
    parser.add_argument("--data-path", type=str, default="C:/visual projects/arb1/data/market_data",
                        help="Path to the market data directory.")
>>>>>>> REPLACE
```
*Примечание: В коде используются прямые слэши, что является кросс-платформенным и корректно работает в Python на Windows.*

## Обоснование
Это изменение сделает поведение скрипта `analyzer` предсказуемым и удобным. Он будет по умолчанию использовать тот же путь, куда проект `collections` сохраняет данные. Это устранит необходимость вручную указывать путь при каждом запуске анализатора.

## Оценка рисков
Риск отсутствует. Изменение затрагивает только строковый литерал со значением по умолчанию.

## План тестирования
1.  Запустить скрипт `python analyzer/run_all_ultra.py` **без** указания аргумента `--data-path`.
2.  Убедиться, что скрипт успешно находит и анализирует данные из `C:\visual projects\arb1\data\market_data`.

## План отката
1.  Вернуть предыдущее значение по умолчанию `../data/market_data` в файле `analyzer/run_all_ultra.py`.